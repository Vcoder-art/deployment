{
  "name": "seo",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "serve:ssr:seo": "node server/server.mjs"
  },
  "private": true,
  "dependencies": {
    "express": "^4.18.2"
  }
}
